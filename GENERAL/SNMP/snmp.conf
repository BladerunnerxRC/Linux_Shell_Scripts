# Default net-snmp client config for optiplex-two
# Leave this essentially empty so snmpd ignores it.
# Keep this empty/minimal so snmpwalk/snmpget don't complain.
# (snmpwalk/snmpget will still work fine.)
